## Lemongrass
WIP

Basically we're making a control plane for claude code using pty+hooks. meaning that we can programmatically control it, just like an api without paying for an api. strictly for personal use.

```shell
make lemongrass
lemongrass up
lemongrass auth
```

auth will open claude code cli -> login into it -> paste the code -> then run.

MIT
